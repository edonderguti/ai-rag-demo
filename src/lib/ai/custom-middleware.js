// Custom middleware for language model
export const customMiddleware = {
  // Add any middleware configurations here
  // Example:
  // beforeRequest: (request) => { ... },
  // afterResponse: (response) => { ... },
};
